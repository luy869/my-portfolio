---
export interface Props {
  text: string;
}
const { text } = Astro.props;
---

<span class="inline-flex items-center rounded-full border border-border-default bg-bg-secondary px-3 py-1 text-xs text-text-muted">
  {text}
</span>
